/** @type {import('next').NextConfig} */
const nextConfig = {
  // Uncomment this if necessary for React Flow
  // transpilePackages: ['reactflow'],
  // Additional configurations can be added here
}

module.exports = nextConfig